bin/

# Checkpoints and other databases the server writes out at runtime, minus the
# hand-added cores that are bundled with the repo on purpose.
*.db
!LambdaCore.db
!jhcore.db
!Minimal.db
# ...and the themed copies of the two big cores that themes/build.sh regenerates from them.
!LambdaCore-ansi.db
!jhcore-ansi.db
